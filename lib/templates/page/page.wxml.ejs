<view class="container">
    <% if (data.isList) { %>
    <oList id="list" length="{{list.length}}" more="{{ canLoadMore }}" empty-icon="error" empty-tip="<%= data.name %>">

    </oList>
    <% } %>
</view>

<van-toast id="van-toast" />

